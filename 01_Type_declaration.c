#include<stdio.h> 

int main(){
    // int a=4; //type declaration (declared a to be int type)
    // float b; //type declaration (declared b to be float type)
    // char c; //type declaration (declared c to be char type)

    // float x=4;   //This is correct
    // float y=x+5; 

    // float y=x+5; //This is wrong as compiler doesnt know what x is as it is declared later on
    // float x=4;
    return 0;
    }
