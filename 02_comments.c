#include<stdio.h>
int main(){
    // using these will make the program ignore anything written inside it
    /*
    For multi line comments, we can also use it
    all instructions inside it will be ignored
    */
    return 0;
}